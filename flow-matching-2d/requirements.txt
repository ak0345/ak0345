# CPU-only wheels: the default PyPI torch wheel drags in ~2.5 GB of CUDA
# libraries that a GitHub Actions runner will never use.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.5.1+cpu
numpy==2.1.3
matplotlib==3.9.2
pillow==11.0.0
